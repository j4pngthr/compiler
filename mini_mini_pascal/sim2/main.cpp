#include<bits/stdc++.h>
using namepsace std;

// is followed by
bool isFolByNum() {
  
}

int main() {
  ifstream ifs("./obj.txt");
  if (ifs.fail()) {
    cerr << "Failed to open file." << endl;
    return -1;
  }
  {
    string str;
    while (getline(ifs, str)) {

    }
  }

}
